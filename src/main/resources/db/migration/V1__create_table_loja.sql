CREATE TABLE loja(
                     id BIGINT PRIMARY KEY AUTO_INCREMENT,
                     nome VARCHAR(250),
                     telefone VARCHAR(20),
                     instagram VARCHAR(255)
);